# Copy to secrets.yaml and fill in. secrets.yaml is git-ignored; never commit it.
wifi_ssid: "your-network"
wifi_password: "your-wifi-password"
ap_password: "fallback-hotspot-password"        # 8+ chars
api_key: "base64-32-bytes"                      # esphome: `openssl rand -base64 32`
web_username: "admin"
web_password: "choose-a-strong-password"
